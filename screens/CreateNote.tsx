import { Text, View, StyleSheet } from "react-native";

export const CreateNote = () => {
  return (
    <View>
      <Text>CreateNote Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
